# Frontend environment. Copy to `.env` to override.
#
# Where the FastAPI backend lives. Defaults to http://localhost:8000 when unset,
# which is the address `uvicorn app.main:app` uses. Inside docker compose the
# browser still talks to the published port, not the container name.
VITE_API_BASE_URL=http://localhost:8000
